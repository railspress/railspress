<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Theme Preview - <%= @theme_name %></title>
  
  <!-- Theme CSS -->
  <style>
    /* Base styles for preview */
    body {
      margin: 0;
      padding: 0;
      font-family: system-ui, -apple-system, sans-serif;
    }
    
    /* Theme CSS from FrontendRendererService */
    <%= @assets[:css].html_safe if @assets && @assets[:css].present? %>
  </style>
</head>
<body>
  <!-- Theme Content -->
  <div id="theme-preview-content">
    <% if @preview_html.present? %>
      <%= @preview_html.html_safe %>
    <% else %>
      <div style="padding: 20px; text-align: center; color: #666;">
        <h2>Preview Not Available</h2>
        <p>Theme: <%= @theme_name %></p>
        <p>Error: No preview HTML generated</p>
      </div>
    <% end %>
  </div>
  
  <!-- Theme JavaScript from FrontendRendererService -->
  <% if @assets && @assets[:js].present? %>
    <script>
      <%= @assets[:js].html_safe %>
    </script>
  <% end %>
</body>
</html>


