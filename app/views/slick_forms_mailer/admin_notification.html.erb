<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #409eff;
      color: white;
      padding: 20px;
      border-radius: 5px 5px 0 0;
    }
    .content {
      background-color: #f9f9f9;
      padding: 20px;
      border: 1px solid #e0e0e0;
      border-top: none;
    }
    .submission-details {
      background-color: white;
      padding: 15px;
      border-radius: 5px;
      margin-top: 20px;
    }
    .field-row {
      padding: 10px 0;
      border-bottom: 1px solid #e0e0e0;
    }
    .field-row:last-child {
      border-bottom: none;
    }
    .field-label {
      font-weight: 600;
      color: #666;
      margin-bottom: 5px;
    }
    .field-value {
      color: #333;
    }
    .footer {
      text-align: center;
      padding: 20px;
      color: #999;
      font-size: 12px;
    }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #409eff;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      margin-top: 15px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1 style="margin: 0;">📋 New Form Submission</h1>
  </div>
  
  <div class="content">
    <p>You have received a new submission for <strong><%= @form[:title] %></strong></p>
    
    <div class="submission-details">
      <p><strong>Submission ID:</strong> <%= @submission[:serial_number] %></p>
      <p><strong>Submitted:</strong> <%= @submission[:created_at] %></p>
      
      <hr style="margin: 20px 0; border: none; border-top: 1px solid #e0e0e0;">
      
      <h3 style="margin-top: 0;">Form Data:</h3>
      
      <% @submission[:response_data].each do |field_name, field_value| %>
        <div class="field-row">
          <div class="field-label"><%= field_name.titleize %></div>
          <div class="field-value"><%= field_value %></div>
        </div>
      <% end %>
    </div>
    
    <% if @submission[:ip_address] %>
      <p style="margin-top: 15px; font-size: 12px; color: #666;">
        <strong>IP Address:</strong> <%= @submission[:ip_address] %><br>
        <strong>Browser:</strong> <%= @submission[:browser] %><br>
        <strong>Device:</strong> <%= @submission[:device] %>
      </p>
    <% end %>
    
    <a href="<%= admin_fluent_forms_entry_url(@submission[:id]) rescue '#' %>" class="button">
      View Full Submission
    </a>
  </div>
  
  <div class="footer">
    <p>Powered by Fluent Forms Pro for RailsPress</p>
  </div>
</body>
</html>

