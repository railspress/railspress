<!DOCTYPE html>
<html>
<head>
  <title>Pixel Code Preview - <%= @pixel.name %></title>
  <%= stylesheet_link_tag "admin/pixel_preview", "data-turbo-track": "reload" %>
</head>
<body class="pixel-preview-body">
  <div class="pixel-preview-header">
    <h1 class="pixel-preview-title"><%= @pixel.name %></h1>
    <div class="pixel-preview-meta">
      Type: <%= @pixel.pixel_type.titleize %> | 
      Position: <%= @pixel.position.titleize %> | 
      Status: <%= @pixel.active? ? 'Active' : 'Inactive' %>
    </div>
  </div>
  
  <% unless @pixel.custom? %>
    <div class="pixel-preview-warning">
      ⚠️ This code is automatically generated based on the pixel ID: <strong><%= @pixel.pixel_id %></strong>
    </div>
  <% end %>
  
  <div class="pixel-preview-code-block">
    <pre class="pixel-preview-code"><%= @pixel_code %></pre>
  </div>
</body>
</html>








