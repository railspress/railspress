<!DOCTYPE html>
<html>
<head>
  <title>Pixel Code Preview - <%= @pixel.name %></title>
  <style>
    body {
      font-family: 'Monaco', 'Courier New', monospace;
      background: #0a0a0a;
      color: #e5e5e5;
      padding: 20px;
      margin: 0;
    }
    .header {
      border-bottom: 2px solid #2a2a2a;
      padding-bottom: 15px;
      margin-bottom: 20px;
    }
    h1 {
      margin: 0 0 5px 0;
      font-size: 18px;
      color: #fff;
    }
    .meta {
      color: #888;
      font-size: 12px;
    }
    .code-block {
      background: #111;
      border: 1px solid #2a2a2a;
      border-radius: 8px;
      padding: 20px;
      overflow-x: auto;
    }
    pre {
      margin: 0;
      white-space: pre-wrap;
      word-wrap: break-word;
      font-size: 13px;
      line-height: 1.6;
    }
    .warning {
      background: #422006;
      border: 1px solid #78350f;
      color: #fbbf24;
      padding: 12px;
      border-radius: 6px;
      margin: 15px 0;
      font-size: 13px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1><%= @pixel.name %></h1>
    <div class="meta">
      Type: <%= @pixel.pixel_type.titleize %> | 
      Position: <%= @pixel.position.titleize %> | 
      Status: <%= @pixel.active? ? 'Active' : 'Inactive' %>
    </div>
  </div>
  
  <% unless @pixel.custom? %>
    <div class="warning">
      ⚠️ This code is automatically generated based on the pixel ID: <strong><%= @pixel.pixel_id %></strong>
    </div>
  <% end %>
  
  <div class="code-block">
    <pre><%= @pixel_code %></pre>
  </div>
</body>
</html>






