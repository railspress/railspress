<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : 'Theme Editor - RailsPress' %></title>
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  
  <!-- Inter Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  
  <!-- Monaco Editor -->
  <script src="https://cdn.jsdelivr.net/npm/monaco-editor@0.45.0/min/vs/loader.js" defer></script>
  
  <!-- SweetAlert2 -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  
  <%= javascript_importmap_tags %>
  
  <%= content_for :stylesheets %>
  
  <style>
    /* Full height layout for Theme Editor */
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
      overflow: hidden; /* Prevent body scroll */
    }
    
    /* Monaco Editor container must fill available space */
    #monaco-container {
      height: 100% !important;
      width: 100% !important;
    }
    
    /* Ensure Monaco Editor fills container */
    .monaco-editor {
      height: 100% !important;
    }
    
    /* File tree styling */
    .file-tree-item {
      cursor: pointer;
      padding: 0.5rem;
      border-radius: 0.375rem;
      transition: background-color 0.15s;
    }
    
    .file-tree-item:hover {
      background-color: rgba(99, 102, 241, 0.1);
    }
    
    .file-tree-item.active {
      background-color: rgba(99, 102, 241, 0.2);
      font-weight: 600;
    }
    
    .file-icon {
      width: 1rem;
      height: 1rem;
      margin-right: 0.5rem;
      transition: transform 0.2s;
    }
    
    /* Tab bar styling */
    .tab-bar {
      display: flex;
      background-color: #f3f4f6;
      border-bottom: 1px solid #e5e7eb;
      overflow-x: auto;
    }
    
    .dark .tab-bar {
      background-color: #1f2937;
      border-bottom-color: #374151;
    }
    
    .tab {
      display: flex;
      align-items: center;
      padding: 0.75rem 1rem;
      border-right: 1px solid #e5e7eb;
      cursor: pointer;
      transition: background-color 0.2s;
      white-space: nowrap;
    }
    
    .dark .tab {
      border-right-color: #374151;
    }
    
    .tab:hover {
      background-color: rgba(99, 102, 241, 0.1);
    }
    
    .tab.active {
      background-color: white;
      border-bottom: 2px solid #3b82f6;
    }
    
    .dark .tab.active {
      background-color: #1f2937;
    }
    
    .tab-close {
      margin-left: 0.5rem;
      padding: 0.25rem;
      border-radius: 0.25rem;
      cursor: pointer;
      opacity: 0.6;
      transition: opacity 0.2s;
    }
    
    .tab-close:hover {
      opacity: 1;
      background-color: rgba(239, 68, 68, 0.1);
    }
  </style>
</head>

<body class="antialiased">
  <%= yield %>
  
  <script>
    // Disable command palette on theme editor pages
    document.addEventListener('DOMContentLoaded', function() {
      const commandPalette = document.querySelector('[data-controller="command-palette"]');
      if (commandPalette && commandPalette.controller) {
        commandPalette.controller.disable();
      }
    });
  </script>
</body>
</html>