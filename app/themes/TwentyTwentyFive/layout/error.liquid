<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>{{ page.title | default: 'Error' }} · {{ site.title }}</title>
  <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
</head>
<body class="nordic-bg">
  <main class="container narrow">
    {{ content_for_layout }}
  </main>
</body>
</html>
