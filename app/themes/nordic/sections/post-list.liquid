{% schema %}
{
  "name": "Post list",
  "settings": [
    { "id":"layout", "type":"select", "options":["list","grid"], "default":"list", "label":"Layout" },
    { "id":"items_per_page", "type":"number", "default":10, "label":"Items per page" },
    { "id":"scope", "type":"select", "options":["all","category","tag","taxonomy","author"], "default":"all", "label":"Scope" }
  ]
}
{% endschema %}

<div class="post-list">
  <article class="card">
    <div class="card-body">
      <h3 class="card-title">Test Post</h3>
      <p class="card-excerpt">This is a test post to see if the Liquid error goes away.</p>
    </div>
  </article>
</div>
